### 接口文档缺失说明
当前您提供的`YApi获取失败`信息不足以生成符合要求的接口测试用例。请您补充以下**完整的接口文档细节**后，我将为您生成严格遵守规范的三段式接口测试内容：

---

#### 需补充的接口信息清单
1. **接口列表**：本次需求涉及的所有后端接口（如「资格查询接口」「会员领取接口」「领取记录查询接口」等）
2. **每个接口的必填项**：
   - HTTP 方法（GET/POST/PUT/DELETE等）
   - 完整接口路径（如`POST /api/v3/wakeup/member/query`）
   - 请求头（是否需要鉴权Token/设备标识/平台限制（安卓专属）等）
   - 请求参数（Query/Path/Body，区分必填/选填，含参数类型、格式约束、业务规则）
   - 响应结构（JSON格式示例，含业务状态码、错误提示信息、核心业务字段）
3. **业务规则补充**（若接口文档未完全覆盖）：
   - 平台限制逻辑（非安卓用户请求的响应）
   - 会员领取的限制条件（如资格有效期、仅限领取一次等）
   - 鉴权Token的具体格式（Bearer+Token/自定义Header等）