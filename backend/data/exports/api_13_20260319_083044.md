## 测试用例 API-VIP-001

**标题**: 赠送会员-有效参数赠送成功
**优先级**: P0
**模块**: 会员赠送
**前置条件**: 有符合唤活条件的安卓单端用户，且用户已登录获取有效Token {{TOKEN}}
**接口路径**: POST /next/app/vip/send/v2

**Headers**:
| Key | Value | 必须 |
|-----|-------|------|
| Authorization | Bearer {{TOKEN}} | 是 |
| Content-Type | application/json | 是 |

**请求体**:
```json
{
  "openday": 8,
  "watermarkCount": 100,
  "sourceDetail": "daily_active_reward_20250317_android"
}
```

### 断言规则
| 断言类型 | 断言路径 | 期望值 | 匹配方式 |
|----------|----------|--------|----------|
| status_code | - | 200 | equals |
| json_field | $.code | 0 | equals |
| response_time | - | 2000 | less_than |

**预期结果**: 返回200，业务码code=0，响应时间小于2s



## 测试用例 API-VIP-002

**标题**: 赠送会员-缺失必填参数openday
**优先级**: P1
**模块**: 会员赠送
**前置条件**: 用户已登录获取有效Token {{TOKEN}}
**接口路径**: POST /next/app/vip/send/v2

**Headers**:
| Key | Value | 必须 |
|-----|-------|------|
| Authorization | Bearer {{TOKEN}} | 是 |
| Content-Type | application/json | 是 |

**请求体**:
```json
{
  "watermarkCount": 100,
  "sourceDetail": "daily_active_reward_20250317_android"
}
```

### 断言规则
| 断言类型 | 断言路径 | 期望值 | 匹配方式 |
|----------|----------|--------|----------|
| status_code | - | 400 | equals |
| json_field | $.code | 40001 | in |
| json_field | $.message | contains "openday" | contains |
| response_time | - | 2000 | less_than |

**预期结果**: 返回400，业务码为参数缺失类错误码，提示包含"openday"，响应时间小于2s



## 测试用例 API-VIP-003

**标题**: 赠送会员-缺失必填参数watermarkCount
**优先级**: P1
**模块**: 会员赠送
**前置条件**: 用户已登录获取有效Token {{TOKEN}}
**接口路径**: POST /next/app/vip/send/v2

**Headers**:
| Key | Value | 必须 |
|-----|-------|------|
| Authorization | Bearer {{TOKEN}} | 是 |
| Content-Type | application/json | 是 |

**请求体**:
```json
{
  "openday": 8,
  "sourceDetail": "daily_active_reward_20250317_android"
}
```

### 断言规则
| 断言类型 | 断言路径 | 期望值 | 匹配方式 |
|----------|----------|--------|----------|
| status_code | - | 400 | equals |
| json_field | $.code | 40001 | in |
| json_field | $.message | contains "watermarkCount" | contains |
| response_time | - | 2000 | less_than |

**预期结果**: 返回400，业务码为参数缺失类错误码，提示包含"watermarkCount"，响应时间小于2s



## 测试用例 API-VIP-004

**标题**: 赠送会员-缺失必填参数sourceDetail
**优先级**: P1
**模块**: 会员赠送
**前置条件**: 用户已登录获取有效Token {{TOKEN}}
**接口路径**: POST /next/app/vip/send/v2

**Headers**:
| Key | Value | 必须 |
|-----|-------|------|
| Authorization | Bearer {{TOKEN}} | 是 |
| Content-Type | application/json | 是 |

**请求体**:
```json
{
  "openday": 8,
  "watermarkCount": 100
}
```

### 断言规则
| 断言类型 | 断言路径 | 期望值 | 匹配方式 |
|----------|----------|--------|----------|
| status_code | - | 400 | equals |
| json_field | $.code | 40001 | in |
| json_field | $.message | contains "sourceDetail" | contains |
| response_time | - | 2000 | less_than |

**预期结果**: 返回400，业务码为参数缺失类错误码，提示包含"sourceDetail"，响应时间小于2s



## 测试用例 API-VIP-005

**标题**: 赠送会员-sourceDetail参数为空字符串
**优先级**: P2
**模块**: 会员赠送
**前置条件**: 用户已登录获取有效Token {{TOKEN}}
**接口路径**: POST /next/app/vip/send/v2

**Headers**:
| Key | Value | 必须 |
|-----|-------|------|
| Authorization | Bearer {{TOKEN}} | 是 |
| Content-Type | application/json | 是 |

**请求体**:
```json
{
  "openday": 8,
  "watermarkCount": 100,
  "sourceDetail": ""
}
```

### 断言规则
| 断言类型 | 断言路径 | 期望值 | 匹配方式 |
|----------|----------|--------|----------|
| status_code | - | 400 | equals |
| json_field | $.code | 40002 | in |
| json_field | $.message | contains "sourceDetail" | contains |
| response_time | - | 2000 | less_than |

**预期结果**: 返回400，业务码为参数格式类错误码，提示包含"sourceDetail"，响应时间小于2s



## 测试用例 API-VIP-006

**标题**: 赠送会员-openday参数传字符串类型
**优先级**: P2
**模块**: 会员赠送
**前置条件**: 用户已登录获取有效Token {{TOKEN}}
**接口路径**: POST /next/app/vip/send/v2

**Headers**:
| Key | Value | 必须 |
|-----|-------|------|
| Authorization | Bearer {{TOKEN}} | 是 |
| Content-Type | application/json | 是 |

**请求体**:
```json
{
  "openday": "8",
  "watermarkCount": 100,
  "sourceDetail": "daily_active_reward_20250317_android"
}
```

### 断言规则
| 断言类型 | 断言路径 | 期望值 | 匹配方式 |
|----------|----------|--------|----------|
| status_code | - | 400 | equals |
| json_field | $.code | 40002 | in |
| json_field | $.message | contains "openday" | contains |
| response_time | - | 2000 | less_than |

**预期结果**: 返回400，业务码为参数格式类错误码，提示包含"openday"，响应时间小于2s



## 测试用例 API-VIP-007

**标题**: 赠送会员-不携带Authorization鉴权Header
**优先级**: P1
**模块**: 会员赠送
**前置条件**: 无
**接口路径**: POST /next/app/vip/send/v2

**Headers**:
| Key | Value | 必须 |
|-----|-------|------|
| Content-Type | application/json | 是 |

**请求体**:
```json
{
  "openday": 8,
  "watermarkCount": 100,
  "sourceDetail": "daily_active_reward_20250317_android"
}
```

### 断言规则
| 断言类型 | 断言路径 | 期望值 | 匹配方式 |
|----------|----------|--------|----------|
| status_code | - | 401 | equals |
| json_field | $.code | 40101 | in |
| json_field | $.message | contains "未授权" | contains |
| response_time | - | 2000 | less_than |

**预期结果**: 返回401，业务码为未授权类错误码，提示包含"未授权"，响应时间小于2s



## 测试用例 API-VIP-008

**标题**: 赠送会员-携带无效Authorization鉴权Header
**优先级**: P1
**模块**: 会员赠送
**前置条件**: 准备一个已过期/伪造/格式错误的Token {{INVALID_TOKEN}}
**接口路径**: POST /next/app/vip/send/v2

**Headers**:
| Key | Value | 必须 |
|-----|-------|------|
| Authorization | Bearer {{INVALID_TOKEN}} | 是 |
| Content-Type | application/json | 是 |

**请求体**:
```json
{
  "openday": 8,
  "watermarkCount": 100,
  "sourceDetail": "daily_active_reward_20250317_android"
}
```

### 断言规则
| 断言类型 | 断言路径 | 期望值 | 匹配方式 |
|----------|----------|--------|----------|
| status_code | - | 401 | equals |
| json_field | $.code | 40102 | in |
| json_field | $.message | contains "无效" | contains |
| response_time | - | 2000 | less_than |

**预期结果**: 返回401，业务码为无效Token类错误码，提示包含"无效"，响应时间小于2s